<% content_for :title, "Comments" %>

<% if notice.present? %>
  <div class="mb-6 px-4 py-3 rounded-lg bg-green-100 text-green-800 border border-green-300">
    <%= notice %>
  </div>
<% end %>

<h1 class="text-3xl font-bold text-indigo-600 mb-6">Comments</h1>

<div id="comments" class="grid gap-6 mb-8">
  <% @comments.each do |comment| %>
    <div class="bg-white p-6 rounded-xl shadow hover:shadow-md transition">
      <%= render comment %>
      <div class="mt-3">
        <%= link_to "💬 Show this comment", comment,
            class: "text-indigo-600 hover:underline text-sm font-medium" %>
      </div>
    </div>
  <% end %>
</div>

<%= link_to "➕ New comment", new_comment_path,
    class: "inline-block bg-indigo-600 text-white px-4 py-2 rounded-lg hover:bg-indigo-700 transition text-sm" %>
